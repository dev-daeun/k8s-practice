apiVersion: v1
kind: Pod
metadata:
  name: conf-from-file
spec:
  volumes:
    - name: mysql-conf-vol       # 컨피그맵 볼륨
      configMap:
        name: mysql-conf         # 키/값을 가져올 컨피그맵 이름
  containers:
    - name: my-container
      image: busybox
      args: ['tail', '-f', '/dev/null']
      volumeMounts:
        - name: mysql-conf-vol    # volumes 에서 정의한 컨피그맵 볼륨 이름
          mountPath: /etc/mysql   # 마운트 된 컨피그맵 경로
